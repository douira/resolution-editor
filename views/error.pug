extends layout

block title
  title Error - Resolution Editor

block content
  .container
    h4.center: +icon("gesture", "large " + weakpermColor)
    h3=error
    h5.grey-text.text-lighten-1 Status #{error.status}
    if message
      p=message
    .divider
    pre=error.stack
